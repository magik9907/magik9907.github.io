import { ActiveFragmentLinkDirective } from './active-fragment-link.directive';

describe('ActiveFragmentLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveFragmentLinkDirective();
    expect(directive).toBeTruthy();
  });
});
